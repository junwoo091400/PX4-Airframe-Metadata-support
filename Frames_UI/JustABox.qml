import QtQuick 2.0

// Test item to check if I can import this (as it exists in the same folder) in the `main.qml` file.

Item {
    Rectangle {
        id: iAmJustABox
        color: "red"
        width: 10
        height: 100
    }
}
